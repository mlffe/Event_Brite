<div class="col-md-6">
  <div class="card flex-md-row mb-4 box-shadow h-md-250">
    <div class="card-body d-flex flex-column align-items-start">
      <h3 class="mb-0">
        <a class="text-dark" href="<%= event_path(event.id) %>"><%= event.title %></a>
      </h3>
      <div class="mb-1 text-muted"><%= event.start_date %></div>
    
      <a href="<%= event_path(event.id) %>">Plus de détails</a>
    </div>
     </div>
</div>
